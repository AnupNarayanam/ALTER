# ============================================================
# QT6CT SETTINGS
# ============================================================
# Mirrors the GTK settings so Qt apps match GTK apps match
# everything else — the whole point of theming Qt at all is
# consistency, not because Qt apps are a primary workflow tool.
# ============================================================

[Appearance]
style=Fusion
icon_theme=Papirus-Dark
custom_palette=true

[Fonts]
general="Inter,10,-1,5,50,0,0,0,0,0"
fixed="JetBrainsMono Nerd Font,10,-1,5,50,0,0,0,0,0"

[Interface]
cursor_flash_time=0
double_click_interval=300
